#postfix:
#  build: postfix
#  links:
#    - dovecot
#    - mysql

dovecot:
  build: dovecot
  links:
    - solr
  volumes:
    - ./test:/var/mail/test
  ports:
    - "127.0.0.1:1143:143"

#mysql:
#  image: mariadb
#  volumes:
#    - ./mysql/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d:ro
#  environment:
#    - MYSQL_ROOT_PASSWORD=password
#    - MYSQL_POSTFIX_PASSWORD=password

solr:
  build: solr
  ports:
    - "127.0.0.1:8983:8983"
